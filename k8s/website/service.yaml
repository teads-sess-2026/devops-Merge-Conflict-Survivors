apiVersion: v1
kind: Service
metadata:
  name: website
  namespace: website
  labels:
    app: website
spec:
  type: LoadBalancer
  selector:
    app: website
  ports:
  - protocol: TCP
    port: 81
    targetPort: 81
    name: http
